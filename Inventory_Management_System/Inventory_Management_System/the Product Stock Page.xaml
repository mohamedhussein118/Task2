<Window x:Class="Inventory_Management_System.the_Product_Stock_Page"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Inventory_Management_System"
        mc:Ignorable="d"
        Title="the_Product_Stock_Page" Height="450" Width="800">
    <Grid Margin="0,-3,0,3">
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>

        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition Height="auto"></RowDefinition>
            <RowDefinition></RowDefinition>
            <RowDefinition></RowDefinition>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Height="30" VerticalAlignment="Top" Background="DarkBlue">
            <TextBlock Foreground="White" Text="Product Stock Section" FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"></TextBlock>
        </Border>
        <DataGrid x:Name="dataGridStock" Grid.Row="1" Grid.RowSpan="2" Grid.Column="0" Margin="10" AutoGenerateColumns="False" IsReadOnly="True"  >
            <DataGrid.Columns>
               
                <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Vertical">
            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Prodect Name:" Margin="0,0,20,30"></TextBlock>
                <TextBox x:Name="txtProductName" Width="200" Height="20" VerticalAlignment="Top"/>
            </StackPanel>
            <StackPanel Grid.Row="4" Grid.Column="0" Orientation="Horizontal">
                <TextBlock Text="Quantity:" Margin="0,0,50,10"></TextBlock>
                <TextBox x:Name="txtQuantity" Width="200" Height="20" VerticalAlignment="Top"/>
            </StackPanel>
            <Button Content="Ship out" Width="100" Margin="70" Background="DarkBlue" Foreground="White" FontWeight="Bold" Click="Button_Click"></Button>

        </StackPanel>
        <Border Grid.Row="0" Grid.Column="1" Height="30" VerticalAlignment="Top" Background="DarkBlue">
            <TextBlock Foreground="White" Text="Low Stock " FontSize="20" FontWeight="Bold" HorizontalAlignment="Center"></TextBlock>
        </Border>
        <DataGrid Name="Low" Grid.Row="1" Grid.RowSpan="2" Grid.Column="1"  Margin="10" AutoGenerateColumns="False" IsReadOnly="True">
            <DataGrid.Columns>

                <DataGridTextColumn Header="Product Name" Binding="{Binding Name}" Width="*"/>
                <DataGridTextColumn Header="Description" Binding="{Binding Description}" Width="*"/>
                <DataGridTextColumn Header="Quantity" Binding="{Binding Quantity}" Width="*"/>
                <DataGridTextColumn Header="Price" Binding="{Binding Price}" Width="*"/>
            </DataGrid.Columns>
        </DataGrid>
        
    </Grid>
</Window>
